# PostgreSQL configuration for the Docker Compose database service.
DATABASE_URL=postgresql://postgres:password@localhost:5432/retailpulse

# Replace this with a long, random value outside local development.
SECRET_KEY=replace-with-a-long-random-secret
